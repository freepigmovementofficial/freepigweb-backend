{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "npm run build && npx prisma migrate deploy && npx prisma generate && npm run prisma:seed && node dist/app.js",
        "restartPolicyType": "ON_FAILURE"
    }
}